# Phase15 optional extras for Windows (CPU-only PyTorch)
# Run from repo root: .\scripts\phase15\install_phase15_extras.ps1

pip install -U pip
pip install node2vec pykeen
pip install torch --index-url https://download.pytorch.org/whl/cpu
pip install torch-geometric

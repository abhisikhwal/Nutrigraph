# nutri-graph

NutriGraph live graph explorer (React + Vite). Deploy notes: [`docs/DEPLOY_GUIDE.md`](../../docs/DEPLOY_GUIDE.md).

```bash
npm install
npm run dev      # local preview
npm run build    # production build to dist/
```
